[package]
name = "lp-pool"
version = "0.1.0"
edition = "2021"

[features]
default = []
alloc-tracking = []  # Enable allocation tracking with type metadata

[dependencies]
thread_local = "1.1"

[dev-dependencies]


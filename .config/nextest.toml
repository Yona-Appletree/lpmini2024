[profile.default]
# Set a per-test timeout of 5 seconds
slow-timeout = { period = "5s", terminate-after = 2 }

[profile.ci]
# Stricter timeout for CI
slow-timeout = { period = "3s", terminate-after = 1 }

